
dataset_path: /workspace/preprocess_step1/preprocess_step1/sensor
checkpoint: /workspace/OpenSceneFlow/checkpoints/seflow_best.ckpt
data_mode: val # [val, test]
save_res: False # [True, False]

leaderboard_version: 1 # [1, 2]
supervised_flag: True # [True, False], whether you use any label from the dataset

# no need to change
save_res_path: 
num_frames: 2
slurm_id: 00000
output: ${model.name}-${slurm_id}
gpus: 1
seed: 42069
eval_only: True
wandb_mode: disabled # [offline, disabled, online]
defaults:
  - hydra: default
  - model: deflow